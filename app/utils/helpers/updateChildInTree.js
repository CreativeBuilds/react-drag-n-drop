/**
 * @desc takes id and content tree and finds the object you're looking for and replaces it with replacement
 */
module.exports = ({ id, children, replacement }) => {
  // Uses Object.assign to replace keys
};
